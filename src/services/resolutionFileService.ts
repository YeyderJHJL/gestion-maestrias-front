/**
 * @deprecated Usa `uploadResolution` de `./filesApiService` directamente.
 * Este módulo se mantiene por compatibilidad mientras se migran los imports.
 */
export { uploadResolution } from './filesApiService';
