# Google OAuth Client ID — obtenerlo de Google Cloud Console
# Copiar este archivo como .env.local y completar el valor
VITE_GOOGLE_CLIENT_ID=xxxx.apps.googleusercontent.com

# URL del backend (solo si no usas el proxy de Vite)
# VITE_API_URL=http://localhost:8080/api
